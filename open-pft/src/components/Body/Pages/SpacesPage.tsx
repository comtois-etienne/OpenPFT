import { IProps, IData } from '../../../classes/PropsInterface'
import './../../../App.css'

function SpacesPage({data} : IProps<IData>) {
   return (
      <div>Spaces</div>
   )
}

export default SpacesPage